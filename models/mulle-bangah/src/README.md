# mulle-bangah `src/`

Parametric cadgen model scripts. Run from repo root:

```bash
npm run agents:python -- models/mulle-bangah/src/assembly.py
```

Parts land in `../STEP/`. See project `README.md` for phase order and energy-chain datums.
