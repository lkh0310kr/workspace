# world-engine-thruster-demo

**Force-mode script** (`script_mode: force`) — dynamic crate hovers with sinusoidal side thrust (Unity `Rigidbody.AddForce` pattern).

```sh
./world-engine/qt-shell/target/debug/world-engine-qt-shell \
  apps/workspace/test-fixtures/world-engine-thruster-demo
```
