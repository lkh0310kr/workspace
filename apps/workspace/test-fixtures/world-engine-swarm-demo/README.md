# world-engine-swarm-demo

**Swarm seek** — one wandering leader + four seekers using `entity_pos(target)`. Zero gravity, pure kinematic AI (Reynolds boids-lite / Unity steering basics).

```sh
./world-engine/qt-shell/target/debug/world-engine-qt-shell \
  apps/workspace/test-fixtures/world-engine-swarm-demo
```
