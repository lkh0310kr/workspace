# world-engine-orbit-demo

Zero-gravity **orbital motion** — moon kinematic body circles a fixed planet using `entity_pos("planet")` (Godot node lookup pattern).

```sh
./world-engine/qt-shell/target/debug/world-engine-qt-shell \
  apps/workspace/test-fixtures/world-engine-orbit-demo
```

Headless: `cargo test orbit_script_circles_planet` in `world-engine/core`.
