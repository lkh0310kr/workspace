# world-engine-patrol-demo

**Waypoint patrol** — guard sphere loops a square path. Uses `global` state + `waypoints` array from `script_args` (persistent script scope, Godot-style).

```sh
./world-engine/qt-shell/target/debug/world-engine-qt-shell \
  apps/workspace/test-fixtures/world-engine-patrol-demo
```
