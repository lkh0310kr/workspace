/** Current `PaneGroupConfig.schemaVersion` written on salvage / new layouts. */
export const PANE_GROUP_SCHEMA_VERSION = 1;

/** On-disk `.workspace/layout.json` envelope version. */
export const LAYOUT_EXPORT_FILE_VERSION = 1;

export const LAYOUT_EXPORT_REL_PATH = ".workspace/layout.json";
