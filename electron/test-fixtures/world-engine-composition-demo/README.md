# world-engine-composition-demo

Phase 41: explicit `components[]` plus flat `body_type: "none"` marker objects.

- `zone_marker` — transform + properties + pick bounds, no physics/render
- `walker` — legacy flat entity with kinematic script

`world-engine-flat-equiv.json` mirrors the components scene for contract tests.
