# TODO
- [ ] 브라우저
    - [ ] 이 외에 브라우저 관련 기능 orca 참고해서 고도화 (지금 너무 불편해) — 다음 후보: 실제 파비콘 표시, 탭별 히스토리 뒤로/앞으로 목록(길게 누르면 드롭다운), 다운로드 UI, 확대/축소
    - [x] Cmd + R, Cmd + Shift + R 등 단축키 생각 후 구현 — 2026-08-24. `@electron-toolkit/utils`의 `optimizer.watchWindowShortcuts`가 프로덕션 빌드에서 Cmd+R 계열을 `before-input-event` 레벨에서 무조건 `preventDefault`하고 있어서(렌더러 DOM에 키 이벤트 자체가 안 옴 — 렌더러 쪽 keydown 리스너로는 절대 못 잡음, 그래서 메인 프로세스에서 직접 가로채서 IPC로 렌더러에 알리는 방식으로 구현), Cmd+R/Cmd+Shift+R을 "포커스된 브라우저 탭 새로고침/캐시 무시 새로고침"으로 재활용함. 브라우저 탭이 하나도 없으면 아무 일도 안 함(앱 전체를 리로드하는 폴백은 의도적으로 안 만듦 — 터미널 세션 UI 상태 다 날아갈 위험).
    - [x] "어느 탭이 포커스인지" 실제 추적으로 개선 — 2026-08-24. 처음엔 "마지막으로 `visible`이 된 webview"라는 근사치로 구현했는데(여러 pane에 브라우저 탭이 동시에 떠 있을 수 있어서 실제 포커스를 안 쓰면 부정확함 — 예: 브라우저 탭을 보다가 옆 split의 에디터 탭을 클릭해도 그 브라우저 탭이 여전히 "active" 취급됨), `<webview>`의 `focus`/`blur` DOM 이벤트 기반으로 1차 교체했었음. 근데 그것만으론 실제로 안 됨 — orca 코드(`bind-browser-page-webview-listeners.ts`) 대조해서 확인한 진짜 원인: `TerminalPane.tsx`/`EditorContent`는 탭이 active가 되는 순간 `term.focus()`/`view.focus()`를 직접 호출해서 실제 DOM 포커스를 탭 선택에 맞춰 옮기는데, `BrowserContent.tsx`는 그 호출이 아예 없어서 사용자가 브라우저 탭으로 전환만 하고 페이지 안을 직접 클릭하기 전까진 `focus` 이벤트 자체가 안 뜸(그래서 activeBrowserWebview가 계속 비어있거나 예전 값에 머무름). 두 가지로 완성: (1) `BrowserContent.tsx`가 `visible`이 될 때 `webview.focus()`를 직접 호출(터미널/에디터와 동일 패턴), (2) `activeBrowserWebview.ts`에 `installBrowserFocusTracking()` 추가 — `document`에 전역 `focusin` 리스너를 하나만 달아서, 포커스가 `.browser-pane-chrome`(브라우저 pane 자기 자신의 크롬) 바깥 아무데나(다른 split의 터미널/에디터 등) 옮겨가면 무조건 클리어함. `<webview>` 게스트 자체의 포커스는 별도 프로세스라 일반 `focusin` 버블링을 안 타서(그래서 `focus` 이벤트를 따로 듣는 거고) 이 전역 리스너로는 못 잡지만, 그 외 모든 pane 콘텐츠 타입(터미널/에디터/사이드바 등)은 진짜 DOM 포커스라 이 하나의 리스너로 다 커버됨 — 각 pane 컴포넌트를 일일이 안 건드려도 됨. App.tsx에서 한 번 설치.
    - **부가 발견/수정**: `watchWindowShortcuts`가 옵션 없이 호출되면 `zoom: false`가 기본값이라 Cmd+'-'/Cmd+'=' 도 항상(dev/prod 무관) 같은 방식으로 차단하고 있었음 — `App.tsx`의 기존 pane 확대/축소 기능(Cmd+'+'/Cmd+'-')이 이 코드가 추가된 시점부터 계속 조용히 막혀있었던 것으로 보임. `optimizer.watchWindowShortcuts(window, { zoom: true })`로 수정.
- [ ] Terminal 이 이상해 ls 도 Operation not permitted 뜨고. 드래그하면 노란색으로 드래그되고 드래그 end했을때 그냥 풀려버려서 텍스트 복사도 안되고. 내생각에 그냥 ui ux문제가 아니라 터미널 설정쪽 문제같아. 아니면 그냥 터미널을 재시작해본다든지
    - [x] 드래그 복사 안되는 것 — 2026-08-24. 코드로 확인된 원인: tmux `mouse on`(스크롤 되게 하려고 이전에 켠 설정) 때문에 드래그가 브라우저 DOM 셀렉션이 아니라 tmux 자체 copy-mode 셀렉션으로 감(노란 하이라이트가 이거). tmux 기본 드래그-끝 바인딩(`copy-selection-and-cancel`)은 OS 클립보드로 직접 안 쓰고 OSC 52 이스케이프 시퀀스로 "터미널"한테 클립보드에 넣어달라고 요청하는 구조인데, xterm.js는 OSC 52를 파싱만 하고 실제 클립보드 접근 권한이 없어서(렌더러 프로세스) 핸들러를 등록해두지 않으면 그냥 씹힘 — 그래서 하이라이트는 뜨는데 복사는 전혀 안 되는 정확히 그 증상. `TerminalPane.tsx`에 `term.parser.registerOscHandler(52, ...)` 추가해서 payload를 base64 디코드 후 새 IPC(`clipboard:write-text`, main에서 Electron `clipboard.writeText`)로 실제 OS 클립보드에 씀. `pty.ts`의 tmux.conf에 `set-clipboard on`도 명시로 추가(tmux가 OSC 52를 실제로 내보내도록 보장 — 최신 tmux는 기본값이긴 하지만 버전 의존 안 하려고 명시). **주의**: tmux.conf의 `-f` 옵션은 tmux 서버가 처음 뜰 때만 적용되는 설정이라, 이미 떠 있는 tmux 서버가 있으면 `tmux kill-server`로 한 번 재시작해야 이 변경이 반영됨. typecheck+build만으로 검증, 실사용(드래그 후 붙여넣기) 확인 필요.
    - [ ] `ls` Operation not permitted — 코드로는 고칠 수 없는 macOS 권한(TCC) 문제로 보임: workspace root가 `~/Documents/...` 밑이고(TCC가 Documents/Desktop/Downloads를 보호 폴더로 취급), dev 모드로 뜨는 Electron 바이너리가 이 앱한테 Full Disk Access(또는 Files & Folders 밑 Documents 폴더 접근)를 아직 허용 안 받은 상태로 보임. 앱 코드가 실제로 잘못 건드리는 게 없어서(pty.ts는 순수 cwd 전달일 뿐) 고칠 코드가 없음 — macOS 시스템 설정 > 개인정보 보호 및 보안 > 전체 디스크 접근 권한(또는 파일과 폴더 > Documents 폴더)에서 Electron(dev) 또는 빌드된 workspace-app에 권한을 직접 부여해야 함. 미해결로 남김.
- [ ] Editor/Makdown
    - [ ] 검색 기능 UI 너무 옛날 스타일인 이슈 -> vscode 비슷한 구조로 ui 개선.
    - [ ] TreeView multi selection (vscode 참고)
- [ ] Workspace
    - [ ] Tab split horizonta/vertical icon이 필요할까? 탭 추가하고 이동하면 될 거 같은데.
    - [ ] MacOS Native Header의 Sidebar Toggle 버튼 hover시 popover selector 표시하여 quick selecting할 수 있도록
    - [ ] TreeView toggle 버튼을 Search icon 오른쪽에 위치하여 패널에 종속되도록. 현재는 한 탭을 범위로 하고 있는 문제.
- [ ] Bullet list raw,preview 간 간격 안 맞음. 그리고 checkbox때와 동일하게 커서가 불렛에 근접한 경우에만 raw로 표시하도록 (`electron/src/renderer/src/markdownLivePreview.ts`, Tauri 때부터 미해결 그대로 포팅됨)
- [x] Markdown Editor Cmd + B등 단축키 기능 추가 — 2026-08-24. CodeMirror 6엔 마크다운 bold/italic 토글 커맨드가 기본 내장 안 되어 있어서 직접 작성(`toggleMarkdownWrap`) — 선택 영역을 `**`/`*`로 감싸거나, 이미 감싸져 있으면 벗김. Mod-b(굵게)/Mod-i(기울임) 매핑.
