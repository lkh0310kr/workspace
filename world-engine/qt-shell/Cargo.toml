[package]
name = "world-engine-qt-shell"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
world-engine-core = { path = "../core" }
glam = { version = "0.33", features = ["bytemuck"] }

[build-dependencies]
cc = "1"
